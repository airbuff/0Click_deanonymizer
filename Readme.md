# Discord Geoguess Bot

A Discord bot that demonstrates a deanonymization attack using Cloudflare caching and OpenStreetMap.

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt